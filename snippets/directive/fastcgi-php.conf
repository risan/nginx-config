# Keep PATH_INFO disabled unless the application explicitly needs it.
# Check that the requested script exists before sending it to PHP-FPM.
try_files $uri =404;
fastcgi_index index.php;
include snippets/directive/fastcgi.conf;
